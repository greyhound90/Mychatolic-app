import 'package:flutter/material.dart';

class BibleColors {
  static const Color lightBackground = Color(0xFFF7F5F2);
  static const Color sepiaBackground = Color(0xFFF4E7D3);
  static const Color darkBackground = Color(0xFF121212);
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSepia = Color(0xFF4A3B2B);
}

class BibleDurations {
  static const Duration apiTimeout = Duration(seconds: 12);
  static const Duration apiRetryDelay = Duration(milliseconds: 350);
}
